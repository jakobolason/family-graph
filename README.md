# XLS family graph generator

A custom family graph generator, given a -- for now -- `.xls` file.
The generations are denoted by the amount of '*' before the name.
Very custom, don't expect anyone to use this
